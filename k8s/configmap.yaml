apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: the-experiment
data:
  ENV: "production"
  LOG_LEVEL: "info"
  DATABASE_URL: "postgresql+asyncpg://experiment:experiment@postgres:5432/experiment"
  REDIS_URL: "redis://redis:6379/0"
