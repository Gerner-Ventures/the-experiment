---
title: "S3.5 — Monitoring & Cost Tracking"
status: done
issue: 5
priority: P1
tags: [stream-3, infra, monitoring, observability]
---

# S3.5 — Monitoring & Cost Tracking

Set up monitoring, logging, and LLM cost tracking for production visibility.

## Health & Readiness

Deep readiness probe checks DB and Redis connectivity. Liveness probe is shallow.

### Acceptance Criteria

- [x] K8s liveness probe functional (`GET /health`)
<!-- specwright:realized-in:PR#54 file:backend/app/api/routes/health.py -->
- [x] Deep readiness probe checks DB and Redis (`GET /health/ready`)
<!-- specwright:realized-in:PR#54 file:backend/app/api/routes/health.py -->
<!-- specwright:realized-in:PR#54 file:chart/the-experiment/templates/backend-deployment.yaml -->

## Structured Logging

JSON-structured logging via structlog with contextvar support.

### Acceptance Criteria

- [x] Structured JSON logging configured via structlog
<!-- specwright:realized-in:PR#54 file:backend/app/logging.py -->
- [x] Key operations emit structured log events (experiment start, round transitions, errors)

## LLM Cost Tracking

Per-experiment, per-agent, per-round token usage and cost tracking with API access.

### Acceptance Criteria

- [x] LLM token usage tracked per experiment, agent, and round
<!-- specwright:realized-in:PR#54 file:backend/app/llm/tracker.py -->
- [x] Cost estimates calculated from token counts (`cost_usd` on every record)
<!-- specwright:realized-in:PR#54 file:backend/app/llm/models.py -->
- [x] Usage API endpoint returns aggregated report (`GET /experiments/{id}/usage`)
<!-- specwright:realized-in:PR#54 file:backend/app/api/routes/experiments.py -->
- [x] Prompt trace API with pagination and filtering (`GET /experiments/{id}/usage/traces`)
<!-- specwright:realized-in:PR#54 file:backend/app/api/routes/experiments.py -->
- [x] Durable usage persistence via Langfuse (S3.6 supersedes in-memory tracker for persistence)
<!-- specwright:realized-in:PR#73 file:backend/app/llm/client.py -->

## Application Metrics

Runtime metrics for operational visibility.

### Acceptance Criteria

- [x] Metrics endpoint exposes active experiments, rounds processed, WS connection count
- [x] Metrics available for K8s HPA or external dashboards

## PostHog Backend Events

Server-side analytics for experiment lifecycle events.

### Acceptance Criteria

- [x] PostHog captures experiment lifecycle events (created, started, round completed, finished)
- [x] PostHog captures error/failure events (LLM failures, agent crashes)

## Dependencies

- S3.3 (K8s manifests) — done
- S3.6 (Langfuse tracing) — done

## Key Files

- `backend/app/api/routes/health.py` — health and readiness probes
- `backend/app/logging.py` — structlog configuration
- `backend/app/llm/tracker.py` — in-memory usage tracker
- `backend/app/llm/models.py` — UsageRecord, UsageSummary, LLMUsage
- `backend/app/api/routes/experiments.py` — usage and trace endpoints
- `backend/app/core/posthog.py` — PostHog client (initialized but unused)
